[package]
name = "fli"
version = "0.1.0"
edition = "2024"


[profile.dev]
panic = "abort"


[profile.release]
panic = "abort"
opt-level = "s" #"z"
lto = true
codegen-units = 1
strip = true   

[dependencies]
libc = {version="0.2.186", default-features = false }

